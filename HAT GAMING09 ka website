<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>HAT GAMING09 - Game & News Hub</title>

<style>
body{
  margin:0;
  font-family: Arial, sans-serif;
  background:#111;
  color:#fff;
  text-align:center;
}
header{
  background:#000;
  padding:20px;
  border-bottom:2px solid #ffcc00;
}
h1{color:#ffcc00;margin:0;}
h2{color:#ff5733;margin-top:40px;}
.container{
  display:flex;
  flex-wrap:wrap;
  justify-content:center;
  gap:15px;
  padding:20px;
}
.card{
  width:300px;
  background:#1e1e1e;
  border:1px solid #333;
  border-radius:8px;
  padding:10px;
  text-align:left;
}
.card img{
  width:100%;
  border-radius:5px;
}
.card h3{font-size:16px;}
.card a{
  color:#00ffcc;
  text-decoration:none;
  font-size:14px;
}
footer{
  background:#000;
  padding:15px;
  color:#aaa;
  font-size:14px;
}
</style>
</head>

<body>

<header>
  <h1>🎮 HAT GAMING09</h1>
  <p>Gaming News & Latest Free Games</p>
</header>

<h2>🔥 Latest Tech / Gaming News</h2>
<div id="news" class="container"></div>

<h2>🚀 Latest Free Games</h2>
<div id="games" class="container"></div>

<footer>
  © 2026 HAT GAMING09 | 🤘🔥Built by H.A.T
</footer>

<script>
/* ========== NEWS (NO API KEY) ========== */
async function loadNews(){
  try{
    const res = await fetch(
      "https://saurav.tech/NewsAPI/top-headlines/category/technology/in.json"
    );
    const data = await res.json();

    const box = document.getElementById("news");
    box.innerHTML = "";

    data.articles.slice(0,6).forEach(n=>{
      const div = document.createElement("div");
      div.className = "card";
      div.innerHTML = `
        <img src="${n.urlToImage || 'https://via.placeholder.com/300'}">
        <h3>${n.title}</h3>
        <a href="${n.url}" target="_blank">Read More</a>
      `;
      box.appendChild(div);
    });
  }catch(e){
    console.log("News error", e);
  }
}

/* ========== GAMES (NO API KEY) ========== */
async function loadGames(){
  try{
    const res = await fetch("games.json");
    const data = await res.json();

    const box = document.getElementById("games");
    box.innerHTML = "";

    data.forEach(g=>{
      const div = document.createElement("div");
      div.className = "card";
      div.innerHTML = `
        <img src="${g.image}">
        <h3>${g.name}</h3>
        <a href="${g.link}" target="_blank">Visit</a>
      `;
      box.appendChild(div);
    });
  }catch(e){
    console.log("Games error", e);
  }
}

/* LOAD */
loadNews();
loadGames();
</script>

</body>
</html>